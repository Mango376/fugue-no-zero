Original prompt: 现在的问题。第一个客人就出现了二级症状，一开始治疗仪是达不到二级的，根本不能治疗，并且治疗按键点不动

- 2026-03-24: 排查到首诊流程的三个根因。
  - AI 患者生成时 `mappingLevels` 没有限制到当前设备等级，首诊可能直接出现 Lv.2。
  - 后台追踪表规范化时接受了 AI 返回的 `mappingLevels`，会把已经收口的等级再次抬高。
  - 治疗页进入条件只看“已确认异常”，没看“是否存在可治疗异常”，会出现进入后按钮不可提交。

- 2026-03-24: 已完成首诊治疗链修复。
  - 新患者初始症状等级会按当前治疗仪模块等级收口。
  - 后台追踪表的 `mappingLevels` 改为锁定患者初始等级，不再接受 AI 改写。
  - 进入治疗页前会先过滤出可治疗项；若当前全都超出等级，会提示先升级设备，不再进入无法提交的治疗页。
  - 读档时会兼容修正旧存档里的首诊高等级症状。
  - `npm run build` 已通过。

- 2026-03-24: 调整了新档启动顺序。
  - 进入新游戏后立即进入流程，并在后台生成当前环境。
  - 当前环境未就绪时，界面显示“正在检测环境数据”。
  - 环境生成完成后才开始后台补患者队列。
  - 患者按顺序一个个生成，生成出第一个后即可接待，其余继续在后台补到 3 个。
  - 问诊链保持为：生成后台追踪表 -> 根据追踪表生成开场 -> 开场结束后生成问诊选项。

- 2026-03-24: 修正了问诊页环境显示。
  - “当前室外环境”改为读取全局当前环境，不再读取患者局部环境。
  - 环境文本在代码侧统一收口：名称最多 10 字，描述最多 30 字。
  - 患者生成 prompt 中的 `environment` 也改为只能写天气、噪音、气味、湿度、光线、震动等环境现象，不能写人物处境。

- 2026-03-24: 修正“治疗后像是跳过患者反馈，直接回到问诊选项”的流程。
  - `submitTreatment()` 现在会把 AI 生成的治疗反馈写入 `consultationHistory`，类型为 `treatment_feedback`，让这段反馈成为后续问诊的正式上下文。
  - `advanceFromFeedback()` 在部分治愈时会先清掉旧问诊选项，再基于这条治疗反馈重新生成下一轮问诊选项，避免沿用治疗前的旧按钮。
  - `npm run build` 已通过。

- 2026-03-24: 调整了主界面的“系统快照”文案。
  - `SYSTEM_SNAPSHOT` 改为治疗指南，直接说明问诊、诊断、治疗、反馈的玩法顺序。
  - 主界面该卡片的副标题改为“查看治疗顺序与上手玩法”。

- 2026-03-24: 将主界面卡片标题从“系统快照”改为“治疗指南”，与当前内容保持一致。

- 2026-03-24: 调整了费用规则。
  - 模组升级费用改为按当前等级收取：Lv.1 -> 2 需 200，Lv.2 -> 3 需 400，Lv.3 -> 4 需 600。
  - 治疗结算改为按治愈项目逐项收费：Lv.1 收 50，Lv.2 收 100，Lv.3 收 50，Lv.4 收 200。
  - 结算仍然只统计本轮真正治好的异常映射，未治好或猜错的项目不会收费。

- 2026-03-24: 去掉了运行时的环境/患者/问诊文本兜底。
  - 当前环境生成失败时不再使用本地随机环境，患者生成也要求当前环境已先由 AI 成功生成。
  - 患者自身环境信息不再回退到全局环境，必须由 AI 一并生成。
  - 问诊开场、患者回复、问诊选项、还款短信都不再回退到本地模板；AI 失败时改为留空并提示错误。
  - 主界面的当前环境显示与追踪表环境来源也去掉了本地环境相位兜底。

- 2026-03-24: 修正了环境标题被截断的问题。
  - 去掉了 `normalizeGeneratedEnvironment()` 里对环境标题的硬截断，改为只清洗文本、不直接裁字。
  - 当环境标题超过 10 字或描述超过 30 字时，当前环境生成会自动重试，而不是把半截标题存进状态。
  - 环境生成 prompt 和患者环境 prompt 都补充了“标题必须完整，不能缺字、不能截断”的约束。

- 2026-03-24: 修正了“环境生成失败后主页一直显示加载中”的表现。
  - 首页环境卡改为只在 `isEnvironmentLoading` 为真时显示加载动画；失败后会显示“环境信息暂不可用”而不是无限加载。
  - 环境标题上限从 10 字放宽到 14 字，减少因标题略长导致的新开局初始化失败。

- TODO: 如果后续希望“高等级症状也能在问诊末尾直接转复诊”，可以再补一个“无可治疗项时直接转复诊/挂号”的流程分支，而不是仅提示升级设备。

- 2026-03-24: 修复了“新开局环境初始化失败：AI 生成的环境描述超过 30 字”的阻断问题。
  - 当前环境生成仍会先严格重试 3 次，优先要求 AI 按 30 字内返回。
  - 到最后一次尝试时，会把轻微超长的环境描述收束成 30 字内的自然短句，而不是直接让新游戏启动失败。
  - `npm run build` 已通过。
- 2026-03-24: 补上了治疗收尾与患者档案库。
  - 部分治愈仍然会先展示患者反馈，再回到问诊页生成新选项继续诊断。
  - 完整结案或转复诊时，会先保留收尾剧情，再进入费用结算弹窗，展示本次治愈等级与获得的信用点。
  - 已完成病例会存入主界面的“患者档案库”，可查看诊断记录、后台摘要与背景故事。
  - `npm run build` 已通过。

- 2026-03-25: 环境生成改为严格 AI 模式，不再使用本地兜底。
  - 删除了 `gameContent.js` 里的 `ENVIRONMENT_PHASES` 预设环境表，环境不再从本地阶段表回退。
  - 当前环境生成不再从描述补标题，也不再在最后一次尝试时回退到本地环境文本；AI 环境缺字段或格式不对会直接重试并最终失败。
  - 患者局部环境也必须由 AI 返回 `name + description`，不再回退到全局环境。
  - 读档跨天时会强制重新生成当前环境，保证继续游戏后的环境仍然来自 AI。
  - `npm run build` 已通过。

- TODO: 如果后续希望“环境失败时整个流程显式锁死”，可以再补一层 UI gating，例如禁用进入 hub/接诊按钮并直接展示环境生成失败态，而不是只依赖候诊队列生不出来。
- 2026-03-25: 继续清理 Synesthesia 的本地内容池与文案兜底。  
  - 删除了 `gameContent.js` 中未再使用的 `CONSULT_OPTION_LIBRARY` 与患者姓名/职业/牵挂/语气数据池，避免误导后续维护。  
  - `generationPrompts.js` 改为环境只返回 `description`，全局环境和患者局部环境都不再生成或保存环境名称。  
  - `promptBuilder.js` 不再在 prompt 中引用环境名称，环境上下文统一改为描述文本。  
  - `useGameLogic.js` 去掉了后台追踪表、问诊选项、患者档案故事的本地补字逻辑；AI 缺字段时现在会直接报错，不再塞默认文案。  
  - `node --check` 已通过 `gameContent.js`、`generationPrompts.js`、`promptBuilder.js`、`useGameLogic.js`；`npm run build` 已通过。  
- 2026-03-25: 调整治疗后反馈链路，治疗结果不再切换到独立结果页。  
  - `submitTreatment()` 现在会直接回到问诊页，把患者反馈写入问诊记录；`partial` 分支会在问诊页继续刷新后续问诊选项。  
  - `complete` 分支会把 AI 生成的结尾剧情追加到问诊记录里，再等待玩家从问诊页进入结算；`revisit` 分支也同样留在问诊页等待进入结算。  
  - 结算从原来的独立 `settlement` 页面改成了问诊页上的结算弹窗；原 `patient_feedback` / `settlement` 模板已停用，仅保留兼容结构。  
  - 存档新增保存 `patientFeedbackOutcome` 与 `pendingSettlementRecord`，并在读档时把旧的 `patient_feedback` / `settlement` 相位收束回问诊页。  
  - `npm run build` 已通过。尝试按 `develop-web-game` skill 跑 Playwright，但本机缺少 `playwright` 依赖，自动化截图验证未完成。  
